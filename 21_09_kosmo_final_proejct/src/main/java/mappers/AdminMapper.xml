<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="AdminDAO">
	 
	<!-- S: 관리자 회원 관리 -->
	<!-- 회원 목록 조회 -->
	<select id ="getMemberList" resultType="member">
	SELECT * FROM CMIS_MEM order by user_id DESC
	</select>
	
	<!-- 회원 상세 조회 -->
	<select id ="getMember" parameterType="member" resultType="member">
	 SELECT * FROM CMIS_MEM WHERE user_id = #{user_id}
	</select>
	
	<!-- 회원 정보 수정 -->
	<update id ="updateMember" parameterType="member">
	UPDATE CMIS_MEM SET 
	member_name = #{member_name},
	member_age = #{member_age},
	member_phone = #{member_phone}
	where user_id = #{user_id}
	</update>
	<!-- E: 관리자 회원 관리  -->
	
	<!-- S: 관리자 통계 -->
	
	<!-- /*회원가입 건수*/ -->
	<select id ="getMemberCount" resultType="member">
	select count(*) from CMIS_MEM;
	</select>
	
	
	
	
	<!-- E: 관리자 통계 -->
</mapper>